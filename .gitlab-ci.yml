before_script:
  - apt-get -qq update && apt-get -qq install npm && apt-get -qq install yarn
  - npm install @angular/cli yarn -g

build:
  stage: build
  script:
    - yarn install
    - ng build
    - docker build -t test .

