before_script:
  - sudo apt-get update && sudo apt-get install nodejs
  - npm install @angular/cli yarn -g

build:
  stage: build
  script:
    - yarn install
    - ng build
    - docker build -t test .

